# 编码任务要求（严格遵循以下规范）
## 1. 功能约束
请完全按照功能规范文件：{{featureSpecPath}} 实现业务逻辑，不得偏离需求，重点关注边界条件。
## 2. 接口约束
请严格对齐接口规范文件：{{apiSpecPath}}，确保请求/响应格式与 OpenAPI 规范一致。
## 3. 数据约束
请遵循数据规范文件：{{dataSpecPath}}，确保数据格式、字段类型符合 JSON Schema 要求。
## 4. 代码约束
请完全按照代码规范文件：{{codeSpecPath}} 编写代码，包括命名、语法、架构组织等。
## 5. 额外要求
- 生成代码后，自动添加必要的注释（函数用途、参数说明）
- 确保代码可运行，无语法错误
- 优先使用项目已有的依赖库，不随意引入新依赖
- 包含基础的异常处理逻辑
- 依赖包、变量检查导入的准确性
